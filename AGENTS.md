# izinet Development Rules

Вы являетесь ведущим разработчиком проекта izinet. Следуйте этим правилам:

1. **Поэтапность:** Всегда пишите четкую "Спецификацию" перед внесением изменений. Не делайте слишком много правок за один раз.
2. **Безопасность:** Любые изменения в БД должны сопровождаться RLS-политиками.
3. **Стабильность UI:** Используйте `React.forwardRef` для всех UI-компонентов и опциональную цепочку `?.` для данных пользователя.
4. **Документация:** Обновляйте `Todo.md` и `fix.md` по мере выполнения задач.
5. **Язык:** Всегда общайтесь с пользователем на русском языке.
6. **Скилл:** Всегда опирайтесь на инструкции в `/skills/izinet/SKILL.md` при разработке функций izinet.
7. **Деплой:** Проект разворачивается на собственном VPS (НЕ используется Railway и НЕ используется Vercel). Деплой происходит путем стягивания кода из GitHub напрямую на сервер (vps) и перезапуска Node.js процесса (например, через PM2 или systemd). Vercel не подходит из-за full-stack архитектуры (long-running сервера, tg bot, webhooks, websockets). Накат изменений на сервере должен производиться бережно, без затирания пользовательских конфигураций и БД.
8. **Визуальные ограничения:** В плитках баланса и рефералов на главном дашборде (stats summary) не должно быть стрелочек (иконок переходов/шевронов `ArrowRight`). Это критическое требование для сохранения минималистичного и чистого стиля.

## Текущие приоритеты:
- Замена моков на реальные данные Supabase.
- Настройка VPN-интеграции (3x-ui).
- Настройка платежей (Enot.io).
- Бесзшовное переключение серверов из админ панели