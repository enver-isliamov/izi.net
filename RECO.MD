# Анализ текущего состояния и рекомендации по бесшовному переключению (Checklist)

Ниже представлена структурированная версия ваших рекомендаций. Я проанализировал текущий архитектурный подход `izinet` и добавил статус реализации и комментарии разработчика по каждому пункту.

## Текущий чек-лист

| Статус | Задача | Комментарий (izinet) |
| :---: | :--- | :--- |
| ✅ | **Центральное хранилище** | Вы уже успешно интегрировали Supabase для хранения данных пользователей, серверов и ключей. |
| ✅ | **Базовая инфраструктура** | Оба сервера активны, на них развернуты панели управления (X-UI) и базовые сервисы (AdGuard, оплата). |
| ✅ | **Единый UUID пользователя** | **В izinet реализовано:** Для каждого устройства генерируется уникальный UUID (привязан к конкретному юзеру и ID). Эти данные надежно хранятся в БД Supabase. |
| ✅ | **Единый клиент / Синхронизация X-UI через API** | **Мой комментарий:** В архитектуре `izinet` на данный момент одно "устройство" (Device) привязывается к *конкретному* серверу (`server_id`). Если вы хотите добиться 100% бесшовности (Load Balance средствами v2rayNG/Hiddify), клиент должен быть добавлен на **оба** сервера с одинаковым UUID. Если вы дадите команду, мы обновим backend (`server.ts`), чтобы он "стучался" в API обеих панелей 3x-ui и создавал/продлял UUID сразу везде. |
| ⚠️ | **Зеркалирование Reality** | **Мой комментарий:** Зеркалирование ключей (Short ID, Private Key, SNI) — это настройка внутри самой панели `3x-ui`, а не в коде биллинга. Вам нужно вручную скопировать настройки Reality Inbound'а с первого сервера на второй, чтобы клиенты не получали таймаут. |
| ✅ | **Smart Subscription (API)** | **В izinet реализовано:** Наш эндпоинт по подписке (`/api/sub/:id`) уже выдает массив конфигураций. Если мы перейдем на модель мульти-размещения (1 юзер на 2 серверах), то саб-линк автоматически начнет выдавать список (основной IP + резервный IP). Приложения вроде Nekobox/v2rayNG/Hiddify поймут это как список нод и сделают бесшовное переключение (Failover или Load Balance). |
| ⚠️ | **Оптимизация для РФ (ТСПУ)** | **Мой комментарий:** Выбор SNI (например, `update.microsoft.com`), открытый `443` порт и Flow (`xtls-rprx-vision`) настраиваются вами напрямую в 3x-ui при создании Inbound. Настройте это 1 раз, и `izinet` будет корректно вытягивать и выдавать эту конфигурацию пользователям. |

---

## Что вам нужно реализовать сейчас (Технический план)

Если цель — **настоящая бесшовность и балансировка (Smart Balance)**, то план действий следующий:

### 1. Синхронизатор (Supabase -> X-UI API)
В текущем состоянии `izinet` не дублирует клиента на запасной сервер, а держит его на том, который был назначен (через `server_id`).
- **Как изменим:** Нам потребуется переписать логику добавления клиента (`/api/subscription/buy`, и скрипты `check_realtime/check_servers`), чтобы мы рассылали POST-запросы `addClient` (с одинаковым email и UUID) на **все** активные серверы.
- Это обсуждаемо — как только скажете "Делаем единую базу на всех серверах", я перепишу backend за несколько шагов.

### 2. Бесшовность через Reality (Настройка панелей X-UI)
Чтобы переключение IP в телефоне было незаметным (без разрыва TCP/Reality сессии), **вручную в X-UI** на обоих серверах сделайте:
1. **Master Key:** Сгенерируйте 1 пару ключей (Private/Public) и поставьте на все сервера.
2. **Dest & Server Names:** Укажите один домен (например, `dl.google.com`).
3. **Short IDs:** Обязательно одинаковые.
*(Код izinet это не делает, это инфраструктурная часть).*

### 3. Формирование ссылки для пользователя
Формат ссылок уже поддерживается `izinet`. При переходе на дублирование серверов, наша ручка `/api/sub/:id` для одного устройства начнет отдавать список (Base64) такого вида:
```text
vless://uuid@185.72.11.57:443?security=reality&sni=google.com&fp=chrome&pbk=ВАШ_КЛЮЧ&sid=ВАШ_ID#Server_1_Main
vless://uuid@194.50.94.28:443?security=reality&sni=google.com&fp=chrome&pbk=ВАШ_КЛЮЧ&sid=ВАШ_ID#Server_2_Reserve
```
Клиенту останется только включить Auto-Test или Load Balance в своем приложении.

### 4. Обход "Белых списков" (Мобильный интернет РФ)
Это чисто инфраструктурная задача:
- Только `443` порт.
- Хорошие SNI (`update.microsoft.com`).
- Flow: `xtls-rprx-vision` (обход детектирования по отпечатку TLS).

> **ВЫВОД:** Документ RECO.MD — это отличный план. С точки зрения архитектуры проекта `izinet`, база уже готова. Остался один шаг: если вы хотите бесшовный автоматический переход, нам нужно будет отменить жесткую привязку `device <-> 1 server` и переписать код так, чтобы мы "размазывали" подписки пользователей по **всем доступным серверам сразу**. Дайте команду, если согласны перестроить логику.
