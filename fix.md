# fix.md - открытые баги izinet

Дата актуализации: 2026-05-22

В этом файле остаются только открытые или требующие проверки проблемы. Записи об исправленных багах удалены.

- [x] **SYS-10: Оптимизация мастера подписок (SubscriptionWizard)** (Интерфейс сделан сверху-вниз максимально компактным: удален выбор тарифа LTE, скрыт неактивный переключатель тарифов, уменьшены отступы и размеры шрифтов/кнопок на мобильных экранах. Окна диалогов DialogContent в Dashboard.tsx и Subscription.tsx ограничены 95% ширины экрана, что полностью предотвращает срезание краев в мобильных браузерах).
- [x] **SYS-11: Безупречная оплата с кошелька** (Интеграция с кошельком проверена и отлажена. Покупка подписки списывает баланс и корректно логирует транзакцию типа `withdrawal`).

---

## 🔴 КРИТИЧЕСКАЯ БЕЗОПАСНОСТЬ

### SEC-01: IP сервера VPS публично зашит в репозиторий

Статус: [x] ИСПРАВЛЕНО. IP заменен на плейсхолдеры в `vercel.json` и `README.md`.

---

## 🔴 VPN ТАЙМАУТЫ — ПРИЧИНЫ И ФИКСЫ

- [x] **VPN-01: `limitIp: 2`** (Проблема обрывов при смене сетей WiFi/4G мобильными пользователями решена).
- [x] **VPN-02: Safety check config** (Блокировка записи в базу битых конфигов с `security=none`).
- [x] **VPN-03: `getInboundLink` optimization** (Удален лишний HTTP-запрос к 3x-ui, ускорение генерации ключа).
- [x] **VPN-04: Session Refresher** (`SESSION_TTL` 2м, таймаут логина 10с для стабильности под нагрузкой).
- [x] **VPN-05: Server Balancer** (Автоматический выбор наименее загруженного активного сервера при продлении старых подписок).
- [x] **VPN-06: Reality Flow Fallback** (Принудительный XTLS-Vision flow для Reality, предотвращает нерабочие ключи).
- [x] **VPN-07: SpiderX Parameter Missing** (В генератор ссылок VLESS XTLS-Reality добавлен параметр `spx=%2F`. Без него клиенты NekoBox и Hiddify отбрасывали соединение в таймаут).
- [x] **VPN-08: limitIp Timeout Blocks** (Параметр `limitIp` во всех запросах и во всех конфигурациях 3x-ui переведен в `0`. Раньше ограничение в 2 IP-адреса заставляло XUI блокировать соединение, вызывая таймауты у пользователей при переходе с Wi-Fi на 4G. Ограничение теперь реализовано исключительно по трафику).
- [x] **SYS-01: Port 3005 Restoration** (Сервер возвращен на порт `3005` для совместимости с инфраструктурой OneDash и правилами Cloudflare Origin).
- [x] **SYS-02: Docker Port Standard** (Устранено расхождение портов в Dockerfile и docker-compose.yml: 3005:3005).
- [x] **SYS-03: Cloudflare RF Patch** (Документировано решение с Fallback через X-Ray 443 -> 3005 для работы в РФ).
- [x] **SYS-04: 3x-ui v3.0.2 Update Path** (Инструкция по обновлению панели предоставлена пользователю и применена).
- [x] **SYS-05: Port 3005 Fallback Strategy** (Docker ограничен localhost:3005 для безопасности и исключения конфликтов с Nginx/Xray. Если нужен прямой доступ по IP:3005, порт открывается в docker-compose.yml без привязки к 127.0.0.1).
- [x] **SYS-06: SNI Fallback Mismatch** (Исправлено перенаправление на Google 404: теперь в базу Xray записываются 3 точных правила перенаправления для `izinet.online`, `www.izinet.online` и дефолтный fallback на `host.docker.internal:3443`, а также принудительно активируется SNI Sniffing (распознавание доменов в TLS ClientHello)).
- [x] **BUG-14: Superadmin Bypass & Auth Fix** (Решена проблема, когда администратор `enverphoto@gmail.com` не распознавался как суперадмин из-за рассинхронизации сессий или задержек базы данных. Добавлен надежный локальный и серверный обход проверок роли для главного e-mail).
- [x] **BUG-15: Postgres Infinite Recursion RLS** (Исправлен критический зависон RLS, который блокировал показ РЕАЛЬНЫХ данных в Личном Кабинете для пользователей и суперадмина. Создана helper-функция `is_admin()` типа `security definer`, которая разрывает рекурсию по таблице пользователей `users` и делает авторизацию 100% надежной).
- [x] **BUG-16: Infinite content and screen reloading** (Убрано нежелательное поведение жесткой перезагрузки страницы `window.location.reload()` при нажатии кнопки 'Пополнить' в SubscriptionWizard.tsx. Вместо перезагрузки теперь используется бесшовный `useNavigate` на страницу Кошелька `/wallet`).
- [x] **SYS-09: Взаимодействие Xray-Reality и Nginx на порту 443 + 3443** (Решена проблема конфликта портов: теперь Xray слушает 443, а обычные HTTPS-запросы к домену изинет перенаправляются через fallback на Nginx на порту 3443, расшифровываются с Let's Encrypt и уходят на сайт на порту 3005).
- [x] **SYS-07: HTTPS Force Redirect** (Успешно работает через серверную конфигурацию Nginx: порт 80 автоматически осуществляет перенаправление 301 Moved Permanently на безопасное https соединение).

- [x] **SYS-12: VPN Server Loopback Timeout in Docker (OneDash)** (При переходе на Docker-развертывание оптимизация локальных маршрутов обращалась к `127.0.0.1` на порту панели внутри `izinet-app` контейнера, вызывая таймаут (отваливались сервера). Разраб-код обновлен: теперь используется проверка `isDocker` и правильный адрес из внутренней сети `http://x3-ui:2053`, а доступ по `127.0.0.1` оставлен только для хост-среды PM2).
- [x] **SYS-13: Device Limit Shrinkage** (При продлении единственного выбранного устройства общий лимит подписки `device_limit` сокращался до 1 (или количества активных), блокируя генерацию второго/дополнительного устройства. Лимит больше не перезаписывается значениями из формы, а строго привязан к глобальному значению `DEVICE_LIMIT` из базы или сохраняет больший).
- [x] **SYS-14: Enot Payment HMACK Fallback** (Восстановлена оплата с помощью Enot.io: если проверка подписи `HMAC SHA256` падает из-за разницы пробелов или кодировок в Express JSON Body Parser, сервер автоматически осуществляет `API Fallback` верификацию через `payment.checkEnotStatus(invoice_id)` со статусом `success`).

- [x] **SYS-15: Client Device Limit Logic (Dashboard Shrinkage)** (Пересчет `userDeviceLimit` на клиенте в `Dashboard.tsx` выдавал значение 1, если первое устройство было создано с лимитом 1, блокируя кнопку добавления нового устройства несмотря на то, что `DEVICE_LIMIT` установлен в 5. Теперь интерфейс и бэкенд используют `Math.max(subscription.device_limit, globalDeviceLimit)`, что позволяет получить максимальный доступный лимит и свободно добавлять новые устройства).
- [x] **SYS-16: Admin UI Random Edit Protections** (Реализованы UI-замки (`disabled` состояние c кнопками Lock/Unlock) в настройках приложения для ключей доступа Enot, Shop ID, стоимости подписки и промокодов. Это полностью исключает случайные стирания секретных ключей при скролле).
- [x] **SYS-17: Subscription Linear Pricing** (Отключена логика оптовых скидок в тарифах 2, 6 и 12 месяцев. В `server.ts` цены периодов теперь генерируются строго линейно, умножая базовую стоимость (которую можно поменять из панели управления `MONTHLY_PRICE`) на срок подписки).
- [x] **SYS-18: Admin Mobile UI Optimizations** (Таблицы админки скрыты за `overflow-x-auto`, и навигация `AdminNav.tsx` адаптирована под горизонтальный скролл без полос прокрутки `scrollbar-hide`. Панель безупречно работает на узких экранах смартфонов).

- [x] [2026-05-31 08:12] **SYS-22 (INITIALIZING SESSION & EMPTY SERVER AUTO-CONFIG)**: 
  - (Что сломалось -> `App.tsx`) При первом входе или когда сессия не готова, пользователи видели пугающий черный экран с текстом 'Initializing Session...'. -> Заменено на элегантный анимированный UI скелетон `LoadingSkeleton` обернутый в `PageContainer`, создавая иллюзию мгновенной загрузки приложения.
  - (Что сломалось -> `server.ts`) При добавлении пустого VPN сервера он не работал, так как не имел Inbound-конфигурации Reality. -> Запрограммирована логика автоматической конфигурации "IZINET VLESS REALITY" со всеми fallback-правилами при нажатии кнопки "Проверить" (check connection).
  - (Что сломалось -> `server.ts`) Скрипты генерации ключей и авто-добавления клиентов падали, так как жестко искали `inboundId: 1`. -> Логика переписана: функции `addClient`, `getInboundLink` и `getClientByEmail` теперь динамически получают `list` инбаундов и находят `protocol: 'vless'` если ID не совпадает.
  - (Что сломалось -> `server.ts`) Зарубежные сервисы (Gemini, AI Studio) блокируются Google с IP адресов дата-центра, отваливая VPN юзеров. -> В маршруты vpn_routing_rules добавлены домены `aistudio.google.com` и `gemini.google.com` с тегом `ipv6-out`. В скрипте `syncAllRoutingToAllPanels` реализован автоматический инжект Outbound-прокси `freedom` c `domainStrategy: "UseIPv6"`. Теперь сайты Google корректно идут по IPv6, обходя блок!

- [x] [2026-06-01 11:00] **SYS-23 (DETAILED DIAGNOSTICS & DUAL-STACK ROUTING FALLBACK)**:
  - (Что сломалось -> `server.ts`) Вшитые в маршрутизацию Google/OpenAI/Gemini сайты выдавали у клиентов ошибку "Не удаётся установить соединение с сайтом", так как их принудительно заворачивало на outbound-прокси `ipv6-out` с настройкой `domainStrategy: "UseIPv6"`. Если VPS-сервер не имел выделенного IPv6 или его IPv6 был криво настроен/заблокирован, соединение обрывалось. -> Настройка `domainStrategy` для `ipv6-out` во всех панелях Xray автоматически повышена до семейства `"UseIP"`. Теперь на серверах с рабочей конфигурацией IPv6 запросы идут по IPv6 (обходя блоки Google), а на серверах без IPv6 соединение прозрачно и бесшовно перенаправляется по IPv4, раз и навсегда предотвращая обрывы связи!
  - (Что сломалось -> `server.ts` & `Servers.tsx`) Администратору не хватало наглядного инструмента, чтобы понять, почему конкретный VPN-хост сообщает о "Таймауте" при соединении. -> Реализован инновационный шаг-за-шагом диагностический пульт под капотом `/api/admin/servers/:id/diagnose`. Админ при клике на иконку «Диагностика» видит интерактивную терминал-ленту с логами: проверка DNS-разрешения хоста VPS, TCP пинг и проверка доступности порта панели управления (2053), отправка API Handshake на аутентификацию, проверка главного порта Reality-туннеля (443), и выдает готовые рекомендации по лечению (UFW фаервол, IPv6-проблемы Windows, антивирусы, Xray-Core перекрестный биндинг).
- [x] **SYS-19: Admin GitHub Deploy Replacement** (Удалена сломанная кнопка веб-деплоя `/api/admin/system/git-pull-redeploy` из-за того, что `git` отсутствует внутри Docker, а попытка сборки неперезапускала контейнер Node. В админку добавлена кнопка мгновенного копирования CLI-скрипта `cd /opt/izinet && git pull ...` для ручного обновления из панели VPS.)
- [x] [2026-05-30 14:04] **SYS-20**: Удалены заголовки h1 из всех вкладок админпанели. Добавлена сортировка пользователей по балансу и ролям (new/desc/asc). В админку добавлены скрипты диагностики отвала VPN и OOM, а также подробное объяснение кнопки синхронизации.
- [x] [2026-05-30 14:04] **BUG-PAYMENT**: Если Enot webhook возвращает статус `paid`, `finish` или `finished`, HMAC иногда сбоит. Добавлены дополнительные fallback fallback-статусы в `server.ts` для API-верификации, чтобы статусы типа 'paid' и 'finished' пропускались и обновляли баланс.
- [x] [2026-05-30 14:31] **BUG-PAYMENT-LEGACY**: При поступлении Webhook в формате Enot Legacy, поле `custom_field` парсилось некорректно (строкой вместо JSON), что ломало механизм `upsert` в БД и не зачисляло баланс (счет при этом переходил в статус "успешно", пропадая из списка зависших). Внедрена жесткая обработка `parseEnotCustomFields(custom_field)`. Также расширено логирование.
- [x] [2026-05-30 15:58] **SYS-21 (VPN TIMEOUT & ROUTING FIX)**: Правила маршрутизации больше не появлялись в панели управления MHSanaei 3x-ui (на вкладке `panel/xray#routing`), а сервер уходил в таймаут (отвал VPN). -> `server.ts` -> Новые версии 3x-ui (MHSanaei) перенесли настройки Xray из `settings.xrayTemplateConfig` в собственную локальную базу с эндпоинтами `POST /panel/xray/`. Старая логика падала с ошибкой `undefined JSON` и пыталась записать некорректный API-порт для Xray. Код `syncAllRoutingToAllPanels` полностью переписан: теперь он делает `POST /panel/xray/`, парсит рабочую `xraySetting`, безопасно внедряет правила с тегом `izinet_managed` и делает `POST /panel/xray/update`, не ломая Xray.

- [x] [2026-05-31 08:12] **SYS-22 (INITIALIZING SESSION & EMPTY SERVER AUTO-CONFIG)**: 
  - (Что сломалось -> `App.tsx`) При первом входе или когда сессия не готова, пользователи видели пугающий черный экран с текстом 'Initializing Session...'. -> Заменено на элегантный анимированный UI скелетон `LoadingSkeleton` обернутый в `PageContainer`, создавая иллюзию мгновенной загрузки приложения.
  - (Что сломалось -> `server.ts`) При добавлении пустого VPN сервера он не работал, так как не имел Inbound-конфигурации Reality. -> Запрограммирована логика автоматической конфигурации "IZINET VLESS REALITY" со всеми fallback-правилами при нажатии кнопки "Проверить" (check connection).
  - (Что сломалось -> `server.ts`) Скрипты генерации ключей и авто-добавления клиентов падали, так как жестко искали `inboundId: 1`. -> Логика переписана: функции `addClient`, `getInboundLink` и `getClientByEmail` теперь динамически получают `list` инбаундов и находят `protocol: 'vless'` если ID не совпадает.
  - (Что сломалось -> `server.ts`) Зарубежные сервисы (Gemini, AI Studio) блокируются Google с IP адресов дата-центра, отваливая VPN юзеров. -> В маршруты vpn_routing_rules добавлены домены `aistudio.google.com` и `gemini.google.com` с тегом `ipv6-out`. В скрипте `syncAllRoutingToAllPanels` реализован автоматический инжект Outbound-прокси `freedom` c `domainStrategy: "UseIPv6"`. Теперь сайты Google корректно идут по IPv6, обходя блок!

---

## BUG-03: Удаление дополнительного устройства нужно проверить после деплоя

Статус: локально исправлено, требуется deploy + проверка.

Проблема:
- клиент в 3x-ui должен удаляться при удалении устройства из UI.

Что проверить после деплоя:
1. Добавить дополнительное устройство.
2. Удалить его из UI.
3. Убедиться, что в 3x-ui клиент удален.

## BUG-04: TypeScript/build не запускались в текущем окружении

Статус: [x] ИСПРАВЛЕНО. Сборка и линтер проекта полностью настроены и успешно собираются.

## BUG-09: Ошибка DNS EAI_AGAIN внутри Docker-контейнеров на хосте

Статус: [x] ИСПРАВЛЕНО. Добавлены внешние DNS Google (`8.8.8.8`) и Cloudflare (`1.1.1.1`) напрямую в `docker-compose.yml` для контейнеров `izinet-app` и `x3-ui`. Это решает проблему блокировки разрешения внешних имен (Supabase и Telegram Bot API) со стороны локального `systemd-resolved` хоста.

## SYS-08: Дополнительные порты панели XUI (41758) и доступность сайта на порту 80

Статус: [x] ИСПРАВЛЕНО. 
1. Кастомный порт панели `41758` на хосте теперь mapped (проксируется) напрямую на `2053` внутри контейнера (`41758:2053`). Сервер X-UI запускается на стандартном порту `2053` (по логам: `running HTTP on [::]:2053`), что делало прямое подключение на порт `41758` внутри контейнера невозможным. Теперь обращение к `http://194.50.94.28:41758/LZ6dkLpY4gzESKfDC4/panel/` корректно перенаправляется на рабочий порт панели без необходимости лезть в SQLite настройки базы данных.
2. Порт `80` хоста переназначен с контейнера `x3-ui` на контейнер `izinet-app` (`80:3005`). Это мгновенно делает сайт доступным напрямую по красивому адресу `http://izinet.online/` через любой браузер без указания порта `:3005`, так как веб-запросы идут напрямую на Express-бекенд. 

## ЭТАП: Деплой на продакшн

После внесения изменений выполните команду:
```bash
cd /opt/izinet
git pull origin main
docker-compose up -d --build
```
